// Empty module used as a Vitest alias for `server-only`. The real
// `server-only` is a side-effect-only Next.js helper that throws on the
// client; under Vitest we don't care about that distinction.
export {};
